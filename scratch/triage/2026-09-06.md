# Nebula triage — 2026-09-06

Gathered 07:00 EDT in `/Users/timbutler/dev/itg/tools/nebula` after `git fetch origin --quiet`.
Counts below are against `origin/main` = `734929b`.

Re-verified 07:03 EDT: every command in this report was re-run and every count, SHA, commit subject,
mtime and PR field below still matches. Nothing was found to be wrong; the three edits made on the
second pass are noted where they occur.

## Needs attention

**Nothing needs attention.** No worktree carries uncommitted work more than a day old, no open PR
has a review comment waiting on a push, and no worktree sits on a branch already merged into main.

Two things are close to the line but do not cross it yet, and are recorded in the sections below
rather than here:

- `fix-sleep-watchdog` has 346 uncommitted lines last touched 2026-09-05 12:41 — 18.4 hours ago.
  It crosses the one-day mark at 12:41 today.
- PR #2 has been idle since 2026-09-01 00:50 (5 days), but it is `MERGEABLE` / `CLEAN` with zero
  reviews and zero comments, so nothing is blocked on a response from its author.

## Working tree — `/Users/timbutler/dev/itg/tools/nebula`

`git status --short`:

```
?? scratch/
```

Only the untracked `scratch/` directory, which is where this report lives. No tracked file is modified.

## Recent history

`git log --oneline -15` (local `main`, which is 5 behind `origin/main` — see below):

```
5418ae4 Log the v0.12.0 release and the behind-its-own-work checkout
1e55534 Release v0.12.0
f38488c Log the done-vs-read status color and the workspace tab tier
5a8acbc A finished turn stays violet until you read it, and the workspace tabs get their own tier
8102fa4 Release v0.11.0
f383616 Log the cloud re-attach, unseen counters, workspace confirm, and notes removal
8df1f89 Re-enter a cloud session, count the finishes you missed, confirm workspace deletes
0361f0a Merge pull request #16 from AgentSystemLabs/worktree-workspace-tabs
c94e6de Log the v0.10.0 merge and the per-checkout port/dev-slot work
6f30a55 Let nebula browser pick its port, and key the dev instance to the checkout
eb91435 Merge origin/main (v0.10.0) into the workspace-tabs prototype
30042e9 Prototype: the Workspaces column becomes a top tab bar
e6d476e Fold the v0.10.0 release into the clean-base entry
fd45d42 Release v0.10.0
249668e Log the worktree relocation, settings reset and tab focus, snapshot re-attach, divider removal, and splash scoping
```

The five commits `origin/main` has that this checkout does not:

```
734929b Merge pull request #1 from butlertg/feat/task-automation
004a0e4 An Automation cheatsheet in the pane, and `e` in the global help
75490a8 Tasks that survive the night: runs end themselves, wrap up, and commit
174a728 Log the task automation work and the paste-then-submit gotchas
5f6fca0 Tasks run themselves: cron schedules, agent loops, and an Automation tab
```

This is a clean fast-forward: local `HEAD` is an ancestor of `origin/main` with nothing of its own,
so `git pull` would take it with no merge. This checkout being behind its own released work is a
known pattern here — `.claude/MEMORY.md` records it for v0.11.0 and v0.12.0, both cut from private
worktrees that pushed to `main` without fast-forwarding the shared tree.

## Worktrees

`git worktree list`:

```
/Users/timbutler/dev/itg/tools/nebula                                   5418ae4 [main]
/Users/timbutler/dev/itg/tools/nebula-worktrees/electric-zebra-gallops  a820531 [feat/task-automation]
/Users/timbutler/dev/itg/tools/nebula-worktrees/fix-sleep-watchdog      d1ff4ac [fix-sleep-watchdog]
```

### `nebula` — branch `main`

- **Uncommitted:** none (untracked `scratch/` only).
- **vs `origin/main`:** 0 ahead, 5 behind. Fast-forwardable.
- **Merged into main:** yes, `HEAD` is an ancestor of `origin/main`.

### `nebula-worktrees/electric-zebra-gallops` — branch `feat/task-automation`

- **Uncommitted:** none (untracked `scratch/` only).
- **vs `origin/main`:** 5 ahead, 1 behind. The single commit behind is `734929b`, the merge commit
  from PR #1; all real work on `main` is already in this branch.
- **vs its remote:** in sync with `origin/feat/task-automation` (0/0), so the PR shows everything
  that exists locally.
- **Merged into main:** no. `git cherry` reports all five commits as unmerged — PR #1 merged an
  earlier stretch of this same branch on 2026-08-31, and these five landed after it.
- Commits ahead:

```
a820531 Log the bypass-permissions modal and how it is answered
b461fdc Unattended runs answer the dialog that was killing them
7ce1e6b Log the gh-upstream and PREFIX traps
1ef70cc Runs that write themselves up: a report, a transcript, and an honest diff
a29ab00 Log the run-journal work: reports, transcripts, and honest diffs
```

### `nebula-worktrees/fix-sleep-watchdog` — branch `fix-sleep-watchdog`

- **Uncommitted:** yes — 7 modified tracked files, 346 insertions / 8 deletions.

```
 crates/nebula-core/src/entities.rs   |  11 ++
 crates/nebula-core/src/protocol.rs   |   8 +-
 crates/nebula-daemon/src/registry.rs | 266 +++++++++++++++++++++++++++++++-
 crates/nebula-daemon/src/store.rs    |  23 ++-
 crates/nebula-tui/src/app.rs         |  10 +-
 crates/nebula-tui/src/event_loop.rs  |   6 +
 crates/nebula-tui/src/ui.rs          |  30 ++++
```

  Newest file mtime is 2026-09-05 12:41 — 18.4 hours before this run, under the one-day threshold
  but only just. mtime is a proxy for when the work was last edited, not proof of it; it is the best
  signal available without a stash or a commit to date.
- **vs `origin/main`:** 11 ahead, 1 behind (again only the PR #1 merge commit).
- **vs a remote:** none. The branch has no upstream and no `origin/fix-sleep-watchdog` exists, so
  the 6 commits on top of `feat/task-automation` live only on this machine.
- **Merged into main:** no.
- Its own 6 commits, above the 5 it inherits from `feat/task-automation`:

```
d1ff4ac Log the wake plan and the pmset weekday spelling
cbc0b00 nebula wake: the pmset line worked out from your own schedules
8fc92b9 Log the keep-awake work and the pmset root limit
31a6a97 Hold the machine awake for as long as a run needs it
e4eee44 Log the sleep watchdog and the launchd migration
e9e7003 Tasks that survive a closed lid: launchd fires them, sleep no longer fails them
```

  This branch is stacked on `feat/task-automation`, so it inherits whatever happens to PR #2.

## Open pull requests — `butlertg/nebula`

`gh pr list -R butlertg/nebula --state open` returns exactly one.

### #2 — Runs that write themselves up: reports, transcripts, and an honest diff

- <https://github.com/butlertg/nebula/pull/2>
- `feat/task-automation` → `main`, author `butlertg`, not a draft.
- Opened 2026-08-31 18:43 UTC, last updated 2026-09-01 00:50 UTC.
- 5 commits, matching the branch listing above.
- **Reviews: none. Comments: none. Review requests: none.**
- `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN`.

Because the PR has no review comments at all, the "no commits since the newest review comment"
condition cannot fire. It is simply an open, conflict-free PR that nobody has looked at for five
days — worth merging or reviewing when convenient, but not stuck on anyone.

Note for anyone re-running this by hand: `-R butlertg/nebula` is required, and this was confirmed
rather than assumed. This checkout's `origin` is `butlertg/nebula` and its `upstream` is
`AgentSystemLabs/nebula`; `gh repo set-default --view` reports no default repository set. A bare
`gh pr list --state open` run during verification returned one PR — `#31 Managed workflows
orchestration (feature proposal)` from `lnmunhoz:feat/workflows` — which belongs to
`AgentSystemLabs/nebula` and has nothing to do with this fork.
