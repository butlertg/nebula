# Nebula triage — 2026-08-31

Generated 2026-08-31 22:13 -0400, after `git fetch origin --quiet`; every claim re-verified
against the repo at 22:14 -0400.
`origin` = butlertg/nebula (`upstream` = AgentSystemLabs/nebula), so all ahead/behind counts below are
against **origin/main = 734929b**.

## Needs attention

Nothing needs attention.

The single open PR is under two hours old with no reviews yet, the one worktree carries nothing but an
untracked scratch file written tonight, and no worktree branch is already merged into main. The one
non-default observation — the main checkout sitting 5 commits behind `origin/main` with a clean tree — is
a plain fast-forward away, and this checkout has lagged behind its own merged work before (see the
v0.12.0 entry in `.claude/MEMORY.md`). Not stuck.

## git status --short

At gather time: clean — no modified, staged, or untracked files in
`/Users/timbutler/dev/itg/tools/nebula`. Re-checked at write time it reports one untracked path,
`?? scratch/` — the directory holding this report, created by this run. Nothing else, tracked or
untracked.

## git log --oneline -15

```
5418ae4 Log the v0.12.0 release and the behind-its-own-work checkout
1e55534 Release v0.12.0
f38488c Log the done-vs-read status color and the workspace tab tier
5a8acbc A finished turn stays violet until you read it, and the workspace tabs get their own tier
8102fa4 Release v0.11.0
f383616 Log the cloud re-attach, unseen counters, workspace confirm, and notes removal
8df1f89 Re-enter a cloud session, count the finishes you missed, confirm workspace deletes
0361f0a Merge pull request #16 from AgentSystemLabs/worktree-workspace-tabs
c94e6de Log the v0.10.0 merge and the per-checkout port/dev-slot work
6f30a55 Let nebula browser pick its port, and key the dev instance to the checkout
eb91435 Merge origin/main (v0.10.0) into the workspace-tabs prototype
30042e9 Prototype: the Workspaces column becomes a top tab bar
e6d476e Fold the v0.10.0 release into the clean-base entry
fd45d42 Release v0.10.0
249668e Log the worktree relocation, settings reset and tab focus, snapshot re-attach, divider removal, and splash scoping
```

Local `main` is at `5418ae4`. `origin/main` has moved on to `734929b` (Merge pull request #1 from
butlertg/feat/task-automation), so the top five commits on the remote — the task-automation series and its
merge — are not yet in this checkout.

## Worktrees

`git worktree list`:

```
/Users/timbutler/dev/itg/tools/nebula                                   5418ae4 [main]
/Users/timbutler/dev/itg/tools/nebula-worktrees/electric-zebra-gallops  a820531 [feat/task-automation]
```

### /Users/timbutler/dev/itg/tools/nebula — `main`

- Uncommitted changes: none tracked. The only untracked path is `scratch/`, this report.
- vs `origin/main`: 0 ahead, **5 behind**. Fast-forwardable.
- Merged into main: n/a (it is main).

### /Users/timbutler/dev/itg/tools/nebula-worktrees/electric-zebra-gallops — `feat/task-automation`

- Uncommitted changes: none tracked. One untracked path, `scratch/` (a single `heartbeat.md`, mtime
  2026-08-31 20:39 tonight) — automation scratch, not stranded work.
- vs `origin/main`: **5 ahead, 1 behind** (behind only by the PR #1 merge commit `734929b`).
- Merged into main: **no**. The five commits ahead are exactly the open PR #2. The branch's earlier work
  did land via PR #1, which is why `origin/main` moved.
- Last commit: `a820531` "Log the bypass-permissions modal and how it is answered", 2026-08-31 20:45 -0400,
  Tim Butler — ~1.5 hours old.

## Open PRs — `gh pr list -R butlertg/nebula --state open`

One open PR.

### #2 — Runs that write themselves up: reports, transcripts, and an honest diff

- <https://github.com/butlertg/nebula/pull/2>
- Branch `feat/task-automation`, author butlertg, not a draft.
- Opened 2026-08-31 18:43 UTC, last updated 2026-09-01 00:50 UTC.
- Reviews: none. Review comments: none. Issue comments: none.
- Checks: no status checks reported on the head commit.
- Mergeability: `MERGEABLE` / `CLEAN` (GitHub reported `UNKNOWN` on the first query, having not yet
  computed the merge commit for a head pushed minutes earlier; it resolved on re-check).
- Commits on the branch:

```
a29ab00 2026-08-31T18:31Z Log the run-journal work: reports, transcripts, and honest diffs
1ef70cc 2026-08-31T18:36Z Runs that write themselves up: a report, a transcript, and an honest diff
7ce1e6b 2026-08-31T18:44Z Log the gh-upstream and PREFIX traps
b461fdc 2026-09-01T00:45Z Unattended runs answer the dialog that was killing them
a820531 2026-09-01T00:45:47Z Log the bypass-permissions modal and how it is answered
```

Since there are no review comments at all, the "no commits since the newest review comment" test cannot
fire; the branch is in any case the most recently pushed thing in the repo.
