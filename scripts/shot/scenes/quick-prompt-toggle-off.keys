# The QUICK PROMPT from the SESSIONS PANEL, aimed at the selected checkout: the target row reads
# `worktree: main` with `[ ] new worktree ^N` unticked, inside the usual accent frame.
Tab
Tab
p
Add a retry to the login redirect
